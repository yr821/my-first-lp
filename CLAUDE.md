# CLAUDE.md

このプロジェクトについて Claude Code (claude.ai/code) へのガイダンスです。

## プロジェクト概要

HTML / CSS / JavaScript で構成されたシンプルなランディングページ（LP）プロジェクトです。
ビルドツールやフレームワークは使用しません。

## ルール

- CSS は `style.css` にまとめる（別ファイルに分割しない）
- JavaScript は `script.js` にまとめる（別ファイルに分割しない）
- デザインはシンプルでモダンにする
- コードのコメントは日本語で書く
- 動作確認用の `console.log` はコミット前に必ず削除する
